import React from 'react';
import { Card } from 'antd';

const Deployments: React.FC = () => {
  return (
    <Card title="Deployments" bordered={false}>
      <p>Deployment management interface - Coming soon!</p>
    </Card>
  );
};

export default Deployments;